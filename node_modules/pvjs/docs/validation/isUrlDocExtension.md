### ``isUrlDocExtension(url)``
Checks if ``url`` ends in `pdf`, `doc`, `docx`, `ppt`, `pptx`.

- `url` `<String>`

```js
if (PV.isUrlDocExtension(url)) {
    console.log(`Success`);
} else {
    console.log(`Failure`);
}
```