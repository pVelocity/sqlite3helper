### ``isInteger(i)``
Checks if ``i`` an instance of ``Number`` and is an integer.

- `i` `<Number>`

```js
if (PV.isInteger(i)) {
    console.log(`Success`);
} else {
    console.log(`Failure`);
}
```