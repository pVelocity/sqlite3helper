### ``sortArrayKeyValueDesc(arr)``
Sorts ``arr`` in descending order based on the ``extName`` property of each element.

- `arr` `<Array>`

```js
var arr = [{
	extName: 'a'
}, {
	extName: 'b'
}];
PV.sortArrayKeyValueDesc(arr);
```