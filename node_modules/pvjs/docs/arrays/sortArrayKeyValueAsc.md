### ``sortArrayKeyValueAsc(arr)``
Sorts ``arr`` in ascending order  based on the ``extName`` property of each element.

- `arr` `<Array>`

```js
var arr = [{
	extName: 'a'
}, {
	extName: 'b'
}];
PV.sortArrayKeyValueAsc(arr);
```