### ``replaceAll(regExp, replace, str)``
Replaces all matched regExp in str with value in replace

- `regExp` `<String>`
- `replace` `<String>`
- `str` `<String>`

```js var str = PV.replaceAll('abc', '123', 'abcdef');
```

```result: str = 123def```