### ``hslToRgb(h, s, l)``
Converts ``h``, ``s``, ``l`` to a color object

```js
'hsl(0, 0, 100)' => 	{
				r: 255,
				g: 255,
				b: 255
			}
```

- `h` `<Number>`
- `s` `<Number>`
- `l` `<Number>`

```js
var colorObj = PV.hslToRgb(0, 0, 100);
```