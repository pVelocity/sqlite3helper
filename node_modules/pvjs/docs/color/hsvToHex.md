### ``hsvToHex(h, s, v)``
Converts ``h``, ``s``, ``v`` to a color hex string

```js
'hsv(0, 0, 100)' => '#FFFFFF'
```

- `h` `<Number>`
- `s` `<Number>`
- `v` `<Number>`

```js
var colorHex = PV.hsvToHex(0, 0, 100);
```